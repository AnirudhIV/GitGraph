# Secrets
.env
backend/.env
frontend/.env

# Python
backend/.venv/
backend/__pycache__/
**/__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/dist/
backend/seed/ts-analyzer/node_modules/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Seed script scratch clones
backend/.repo-cache/

# Zero-setup CLI's local SQLite export (backend/cli) -- can land in any
# repo `python -m cli ingest` is pointed at, including this one.
.gitgraph/

# pip build metadata from `pip install -e backend/` (backend/pyproject.toml)
backend/*.egg-info/
backend/build/
backend/dist/

# Third-party Claude Code skills installed via `npx skills add` -- personal
# tooling, not project code.
.claude/skills/
.agents/
skills-lock.json
